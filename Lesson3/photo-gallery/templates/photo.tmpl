<html>
   <head>
        <meta charset="utf-8">
        <title>{{ title }}</title>
        <link rel="stylesheet" href="css/style.css">
   </head>
   <body>
        <div class="catalogue">
            <h1>{{ title }}</h1>
            <p><a href="photo-gallery.php">Back to gallery</a></p>
            <div class="product-fullsize">
                <img src="{{ image }}" alt="{{ title }}"/>
            </div>
        </div>    
    </body> 
</html>